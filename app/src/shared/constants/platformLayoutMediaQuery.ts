/** Align with Sass `$bp-nav-up` (`_breakpoints.scss`). Used for coarse vs fine layout (e.g. skip keyboard steal on phones). */
export const PLATFORM_NAV_UP_MEDIA_QUERY = '(min-width: 769px)' as const
