/** Elements marked with `data-livechat-cursor-isolate` (floating shell, drawer, modals). Section snap scroll ignores wheel here; HOME cursor suspend uses same subtree. */
export const LIVECHAT_DOM_ISOLATE_SELECTOR = '[data-livechat-cursor-isolate]'
