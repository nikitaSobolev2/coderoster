'use client'

/** Barrel only — implementation lives in `InCourseShell.client.tsx` so ESLint resolves TRPC bundle types (typescript-eslint quirk on large `index.tsx`). */
export { default } from './InCourseShell.client'
export type { Props } from './InCourseShell.client'
