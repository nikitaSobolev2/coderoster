/** Public marketing URL for self-serve / plan comparison (not `/admin/plans`). */
export const PLANS_PAGE_HREF = '/plans' as const

/** Platform header / footer / menus — title case. */
export const PLANS_NAV_LABEL = 'Тарифы'

/** Home landing side nav + mobile sheet — matches existing lowercase rhythm. */
export const PLANS_HOME_NAV_LABEL = 'тарифы'

/** Footer column link can spell out intent for first-time visitors. */
export const PLANS_FOOTER_LINK_LABEL = 'Тарифы и подписка'
